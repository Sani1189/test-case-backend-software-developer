# Headless build: this is a server-side batch job with no display, and the GUI build
# drags in X11 libraries the container would never call.
opencv-python-headless>=4.10.0.84
numpy>=2.0.0
shapely>=2.0.6
pydantic>=2.9.0
pyyaml>=6.0.1
pytest>=8.0.0
requests>=2.32.3
